# To Do Items

A list of tasks we should eventually implement when the time is right

- Revisit chat-turn branching: edit/resend should use server-owned ancestry and selected paths, not client-truncated history. See `docs/LibreChat Edit Message Architecture Redesign.md`.
