export const dynamic = "force-dynamic"

// The Chat surface is mounted by the persistent (chat)/layout.tsx — see its
// header comment (adoption-loss fix): a page-owned <Chat/> is torn down when
// the router commits a segment swap, killing a live first-turn stream.
export default function Home() {
  return null
}
